version: '3.9'

services:
  ansible:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: facturation_pme_ansible
    volumes:
      - ./:/ansible
      - ~/.ssh:/root/.ssh:ro
    working_dir: /ansible
    entrypoint: ansible-playbook
